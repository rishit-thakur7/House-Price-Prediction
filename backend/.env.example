# Copy this to .env and fill in values before running the backend

# path to serialized sklearn model and encoder
MODEL_PATH=model.pkl
ENCODER_PATH=encoder.pkl

# comma-separated list of allowed CORS origins (use * for all)
CORS_ORIGINS=*

# optional port (uvicorn will use 8000 by default)
PORT=8000
